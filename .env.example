# Copy this file to  .env  for local `vercel dev`, and set the SAME two values
# in your Vercel project (Settings -> Environment Variables) for production.
# .env is gitignored so the key stays private.

JSONBIN_MASTER_KEY=your_jsonbin_master_or_access_key
JSONBIN_BIN_ID=your_bin_id
